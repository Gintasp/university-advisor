import React from 'react';
import { render } from 'react-dom';

const App = () => (
    <React.Fragment>
        <h1>Comparison</h1>
    </React.Fragment>
);

render(<App />, document.getElementById('comparison'));
